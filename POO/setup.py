from setuptools import setup, find_packages

setup(
    name='Python POO',
    version='1.0.0',
    description='Python project',
    author='Il Vecchio Dev',
    author_email='ilvecchio@gmail.com',
    packages=find_packages(),
    install_requires=[
        'setuptools',  # Especificando setuptools como uma dependência
    ],
)